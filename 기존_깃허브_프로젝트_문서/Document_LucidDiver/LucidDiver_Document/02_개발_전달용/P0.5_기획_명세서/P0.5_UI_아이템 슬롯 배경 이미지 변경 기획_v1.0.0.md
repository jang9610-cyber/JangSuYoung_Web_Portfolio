# P0.5_UI_아이템 슬롯 배경 이미지 변경 기획_v1.0.0

**문서 버전 관리**

| 버전 | 작성 일자 | 작업 내역 |
| --- | --- | --- |
| v1.0.0 | 2026.07.06 | 기획 문서 초안 생성 |

# 아이템 등급에 따른 슬롯 배경 변경

아이템이 장착된 슬롯의 배경 이미지를 장착된 아이템의 등급 값에 따라 변경한다.

## 배경 이미지 변수 연결

InventorySlotUI 스크립트에 배경 이미지와 관련된 변수를 추가한다.

- 슬롯 배경 이미지 오브젝트: slotBGImg (Image) 변수를 추가하고, UI-InventorySlot 오브젝트의 Image 컴포넌트를 연결한다.

![UI_Empty_Item_Slot.png](../../07_기획서_이미지_자료/UI_Empty_Item_Slot.png)
[빈 칸 슬롯 이미지]

![UI_Normal_Item_Slot.png](../../07_기획서_이미지_자료/UI_Normal_Item_Slot.png)
[normal 슬롯 이미지]

![UI_Uncommon_Item_Slot.png](../../07_기획서_이미지_자료/UI_Uncommon_Item_Slot.png)
[uncommon 슬롯 이미지]

![UI_Rare_Item_Slot.png](../../07_기획서_이미지_자료/UI_Rare_Item_Slot.png)
[rare 슬롯 이미지]

![UI_Epic_Item_Slot.png](../../07_기획서_이미지_자료/UI_Epic_Item_Slot.png)
[epic 슬롯 이미지]

![UI_Legend_Item_Slot.png](../../07_기획서_이미지_자료/UI_Legend_Item_Slot.png)
[legend 슬롯 이미지]

- 등급별 슬롯 배경 스프라이트
    - 빈 칸: slotSprite_empty (sprite) 변수를 추가하고, 빈 칸 슬롯에 사용할 이미지 스프라이트를 입력한다.
    - normal 아이템: slotSprite_normal (sprite) 변수를 추가하고, normal 등급 아이템을 장착한 슬롯 배경에 사용할 이미지 스프라이트를 입력한다.
    - uncommon 아이템: slotSprite_uncommon (sprite) 변수를 추가하고, uncommon 등급 아이템을 장착한 슬롯 배경에 사용할 이미지 스프라이트를 입력한다.
    - rare 아이템: slotSprite_rare (sprite) 변수를 추가하고, rare 등급 아이템을 장착한 슬롯 배경에 사용할 이미지 스프라이트를 입력한다.
    - epic 아이템: slotSprite_epic (sprite) 변수를 추가하고, epic 등급 아이템을 장착한 슬롯 배경에 사용할 이미지 스프라이트를 입력한다.
    - legend 아이템: slotSprite_legend (sprite) 변수를 추가하고, legend 등급 아이템을 장착한 슬롯 배경에 사용할 이미지 스프라이트를 입력한다.

## 배경 이미지 출력

슬롯의 아이템 장착 상태가 변경되었을 때, 아이템 데이터에 따라 슬롯 배경 이미지에 출력되는 스프라이트를 변경한다.

- Enums 스크립트에 ItemGrade (enum) 변수를 추가한다.

```csharp
// 아이템 등급 (NEW!!)
public enum ItemGrade
{
	empty = 0,     //빈 칸 (default)
	normal = 1,    //일반 등급
	uncommon = 2,  //고급 등급
	rare = 3,      //희귀 등급
	epic = 4,      //서사 등급
	legend = 5     //전설 등급
}
```

- ItemData 클래스에 itemGrade 변수를 추가한다.

```csharp
using UnityEngine;
using UnityEngine.AddressableAssets;
public abstract class ItemData : ScriptableObject
{
    [Header("아이템 기본 정보")]
    public int TID;                               // 아이템 고유 ID
    public string itemName;                       // 아이템 이름
    public string desc;                           // 아이템 설명
    public itemCategory category;                 // 아이템 분류
    public ItemGrade itemGrade;                   // 아이템 등급 (NEW!!)
    public int effectID;                          // 아이템 효과 ID
    public int itemMultiple;                      // 최대 스택 수량

    [Header("연관 파일 주소")]
    public AssetReferenceSprite icon;                   // 아이템 아이콘
    public AssetReferenceGameObject itemPrefabRef;      // 아이템 프리팹 주소
}
```

- InventorySlotUI 스크립트의 UpdateSlot 메소드에 아이템 등급을 받아 슬롯 배경 이미지를 변경하는 코드를 추가한다.
    - 아이템 등급을 받을 수 없는 경우(grade == null) 배경 이미지를 빈 슬롯으로 출력하는 예외 처리 코드를 적용한다.

```csharp
	[SerializeField] private Image slotBGImg;             //슬롯 배경 이미지
	[SerializeField] private Sprite slotSprite_empty;     //빈 슬롯 스프라이트
	[SerializeField] private Sprite slotSprite_normal;    //일반 아이템 슬롯 스프라이트
	[SerializeField] private Sprite slotSprite_uncommon;  //고급 아이템 슬롯 스프라이트
	[SerializeField] private Sprite slotSprite_rare;      //희귀 아이템 슬롯 스프라이트
	[SerializeField] private Sprite slotSprite_epic;      //서사 아이템 슬롯 스프라이트
	[SerializeField] private Sprite slotSprite_legend;    //전설 아이템 슬롯 스프라이트
    
    public void UpdateSlot(int stack, Sprite sprite, ItemGrade grade)
    {
        if (stack <= 0 || sprite == null || grade == null)  // grade가 없는 경우 예외처리 (NEW!!)
        {
            itemImg.enabled = false;
            itemStack.text = "";
            slotBGImg.sprite = slotSprite_empty;  //grade가 없는 경우 빈 칸으로 출력 (NEW!!)
            return;
        }

        itemImg.enabled = true;
        itemImg.sprite = sprite;
        itemStack.text = stack.ToString();
        
        // {grade 값에 따라 SlotBGImage에 출력하는 스프라이트 파일을 변경한다} (NEW!!)
        SlotBGImg.sprite = (grade) switch
        {
	        ItemGrade.normal   => slotSprite_normal,
	        ItemGrade.uncommon => slotSprite_uncommon,
	        ItemGrade.rare     => slotSprite_rare,
	        ItemGrade.epic     => slotSprite_epic,
	        ItemGrade.legend   => slotSprite_legend,
	        _                  => slotSprite_empty,
        };
    }
```

- InventoryUI의 UpdateSlot(int slotNum, InventorySlotData slotData) 메소드에 아이템 등급을 읽어 InventorySlotUI에 전달하는 코드를 추가한다.

```csharp
    public void UpdateSlot(int slotNum, InventorySlotData slotData)
    {
        InventorySlotUI slotUI = slotsObj[slotNum].GetComponent<InventorySlotUI>();
        // {slotData에 연결된 itemData에서 itemGrade를 받아 slotUI에 전달한다} (NEW!!)
        slotUI.UpdateSlot(slotData.amount, slotData.icon, slotData.itemData.itemGrade);
    }
```

- InventorySlotUI의 UpdateSlot 메소드를 발동시키는 이하의 메소드에도 동일한 변경 사항을 적용한다.
    - InventorySlotUI.Initialize
        
        ```csharp
            public void Initialize(int index)
            {
                slotIndex = index;
                // 빈 칸이므로 ItemGrade.Empty을 전달해 초기화한다 (NEW!!)
                UpdateSlot(0, null, ItemGrade.Empty);
            }
        ```
        
    - InventoryUI.UpdateSafeSlot
        
        ```csharp
            public void UpdateSafeSlot(int slotNum, InventorySlotData slotData)
            {
                InventorySlotUI slotUI = safeSlotsObj[slotNum].GetComponent<InventorySlotUI>();
                // {slotData에 연결된 itemData에서 itemGrade를 받아 slotUI에 전달한다} (NEW!!)
                slotUI.UpdateSlot(slotData.amount, slotData.icon, slotData.itemData.itemGrade);
            }
        ```
        
    - ResultUI.UpdateSafeSlot
        
        ```csharp
            public void UpdateSafeSlot(int slotNum, InventorySlotData slotData)
            {
                InventorySlotUI slotUI = safeSlotsObj[slotNum].GetComponent<InventorySlotUI>();
                // {slotData에 연결된 itemData에서 itemGrade를 받아 slotUI에 전달한다} (NEW!!)
                slotUI.UpdateSlot(slotData.amount, slotData.icon, slotData.itemData.itemGrade);
            }
        ```
        
    - StorageInventoryUI.RefreshInventorySlot
        
        ```csharp
            private void RefreshInventorySlot(InventorySlotUI slotUI, InventorySlotData slotData)
            {
                // {기존 InventorySlotUI의 표시 함수 재사용}
                slotUI.UpdateSlot(slotData.amount, GetIcon(slotData.TID), slotData.itemData.itemGrade);
            }
        ```