# 📝 개발일지 — 우성혁 (v1.0.0)

**작성일**: 2026-06-25
**작성자**: 우성혁
**스프린트**: 2

---

## 📌 오늘의 목표
- "로비 ▷ 출격 준비 화면 → 인게임 세션 ▷ 결과 창 → 로비" 루프가 자연스럽게 연결되도록 개발

---

## ✅ 완료한 작업
### 1. LobbyScene → DemoScene 전환 시 플레이어 정상 스폰 및 컴포넌트 재연결
- LobbyScene에서 DemoScene 로드 시 각 컴포넌트를 태그 기반으로 추적·재연결 구현
- PlayerSpawnPool, EnemySpawnPool, PlayerCam에 태그 부착
- 재진입 시 PlayerStatus 및 ResultServiceLocator 복구 로직 추가
- `IResultService`에 자기 자신을 탐지해 선택적 Unregister 메서드 추가

### 2. UIManager 충돌 해결 및 UI 병합
- 양쪽 씬에 있던 UIManager 중 하나만 남기고 DontDestroyOnLoad 충돌 해소
- DemoScene Manager 오브젝트에 로비 UI 프리팹 연결

### 3. 동조율 단계 누적 저장 및 결과창 연동
- GameManager에 동조율 단계(`linkRateLevel`) 누적 저장 로직 추가
- DemoScene 탈출 결과창 ‘Back to Lobby’ 버튼 터치 시 GlobalEventBus 통해 전달 구현

---

## 🔄 진행 중인 작업
- 로비 씬에서 다이버/기록 UI의 new 레드닷 출력 여부 결정 코드 수정
  - GameManager의 `linkRateLevel`와 `MemoryLogUnlocked` 값을 DiverRecordUI에 전달 중

---

## 💡 메모 / 내일 할 일
- DemoScene 탈출 시 GameManager 이벤트로 전달된 동조율 단계 및 심상 기록 해금 bool을 DiverRecord에 전달해 기록 보기 버튼에 적용
- 출격 준비 화면에서 소비 기물 퀵슬롯(index, icon, count) 데이터를 UI에 출력하는 기능 구현

---
